# E-commerce Core

Учебный проект по объектно-ориентированному программированию.

## Описание

Реализовано ядро интернет-магазина (e-commerce), включающее базовые
сущности: товары и категории.

Проект построен с использованием src-layout и современных инструментов
разработки.

## Реализованный функционал

### Класс Product

- `name` — название товара  
- `description` — описание  
- `price` — цена (float)  
- `quantity` — количество в наличии (int) 

Атрибуты класса автоматически обновляются при создании новых объектов.

### Класс Category

- `name` — название категории  
- `description` — описание категории  
- `products` — список объектов `Product`  
- `category_count` — общее количество созданных категорий  
- `product_count` — общее количество товаров во всех категориях  

Атрибуты класса автоматически обновляются при создании новых объектов.

### 📦 Загрузка данных

Реализована функция:

```python
load_categories_from_json(file_path: str) -> list[Category]
```

## Тестирование

- Проверка инициализации Product
- Проверка инициализации Category
- Проверка счётчиков категорий и товаров
- Проверка загрузки данных из JSON
- Проверка функции demo()
- Покрытие кода тестами — 100%

## Используемые технологии

- Python 3.13
- Poetry
- Pytest
- Pytest-cov
- Flake8
- Black
- isort
- mypy (strict mode)

## Запуск проекта

Установка зависимостей:

```bash
poetry install
```
Запуск тестов с покрытием:

```bash
poetry run pytest --cov=src
```

Проверка типизации:

```bash
poetry run mypy src
```

Форматирование кода:

```bash
poetry run black .
poetry run isort .
```

Запуск демонстрации:

```bash
poetry run python src/main.py
```
---
